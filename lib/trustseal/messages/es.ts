// ─────────────────────────────────────────────────────────────────
// lib/trustseal/messages/es.ts  (asq-trustseal-public-launch)
// Spanish (Español) catalogue. SCAFFOLD: values are currently English placeholders
// so every key resolves on the live site. Replace each string value with a
// professional Spanish translation — the SHAPE must match Messages. Drop-in.
// ─────────────────────────────────────────────────────────────────
import { en } from './en'
import type { Messages } from './en'

// TODO(i18n): replace English values with professional Spanish translations.
export const es: Messages = en
